import React from 'react';

const Home = () => {
  return (
    <div className="home container">
      <p className="content">
        You might already know how to use React Router to set up routing between
        components. But do you know what's happening under the hood? React
        Router regularly introduces breaking changes and headaches for devs
        around the globe. The good new is that a little bit of exploration can
        help us mend those breaks more quickly and knowledgeably.
      </p>
    </div>
  );
};

export default Home;
